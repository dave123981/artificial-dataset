[build-system]
requires = ["setuptools>=68", "setuptools-scm>=8", "wheel"]
build-backend = "setuptools.build_meta"

# --------------------------------------------------
# Project metadata
# --------------------------------------------------

[project]
name = "artificial-dataset"
description = "Artificial dataset generation from Gaussian noise, linear, polynomial, and sinusoidal components."
readme = "README.md"
license = "GPL-3.0"
license-files = ["LICENSE*"]
authors = [
    {name = "Balazs Paszkal Halmos"}
]

requires-python = ">=3.12"

keywords = [
    "pytorch",
    "dataset generation",
    "classification",
    "anomaly detection",
    "signal processing"
]

classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Operating System :: OS Independent",
    "Topic :: Scientific/Engineering :: Artificial Intelligence"
]

dynamic = ["version", "dependencies", "optional-dependencies"]

# --------------------------------------------------
# URLs
# --------------------------------------------------

[project.urls]

Homepage = "https://github.com/dcintlab/image-processing"
Repository = "https://github.com/dcintlab/image-processing"
Issues = "https://github.com/dcintlab/image-processing/issues"

# --------------------------------------------------
# Dynamic versioning from git tags
# --------------------------------------------------

[tool.setuptools_scm]
fallback_version = "0.0.0"

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}

[tool.setuptools.dynamic.optional-dependencies]
dev = {file = ["requirements-dev.txt"]}
docs = {file = ["requirements-docs.txt"]}

# --------------------------------------------------
# Pytest configuration
# --------------------------------------------------
[tool.setuptools.packages.find]
where = ["."]
include = ["artificial_dataset"]

[tool.pytest.ini_options]

testpaths = [
    "tests"
]

addopts = [
    "-v",
    "--strict-markers",
    "--disable-warnings"
]

# --------------------------------------------------
# Coverage configuration
# --------------------------------------------------

[tool.coverage.run]

source = ["artificial_dataset"]

omit = [
    "tests/*"
]

# --------------------------------------------------
# Black formatting
# --------------------------------------------------

[tool.black]

line-length = 88
target-version = ["py312"]

# --------------------------------------------------
# Ruff linter
# --------------------------------------------------

[tool.ruff]

line-length = 88

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "F",    # pyflakes — undefined names, unused imports
    "I",    # isort — import ordering
    "N",    # pep8-naming — class/function/variable naming conventions
    "D",    # pydocstyle — docstring style
    "UP",   # pyupgrade — modern Python syntax
    "B",    # flake8-bugbear — common bugs and design issues
    "C4",   # flake8-comprehensions — idiomatic list/dict/set comprehensions
    "SIM",  # flake8-simplify — flags unnecessarily complex code
    "RUF",  # Ruff-specific rules
]
ignore = ["N812"]

[tool.ruff.lint.pydocstyle]
convention = "numpy"

# --------------------------------------------------
# MyPy typing
# --------------------------------------------------

[tool.mypy]

python_version = "3.12"
strict = true
ignore_missing_imports = true

# .flake8 (for Flake8)
[flake8]
ignore = ["N812"]
